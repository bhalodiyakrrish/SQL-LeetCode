SELECT MAX(Salary) AS SecondHighestSalary
FROM Employee
WHERE Salary < (SELECT MAX(salary) FROM Employee);

Employee =
| id | salary |
| -- | ------ |
| 1  | 100    |
Output
| SecondHighestSalary |
| ------------------- |
| null                |
Expected
| SecondHighestSalary |
| ------------------- |
| null                |
